namespace Enums
{
    public enum CharacterStatId
    {
        Player = 1,
        Count
    }

    public enum PlayerState
    {
        Idle,
        Move,
        Roll,
        Count
    }

    public enum EffectId
    {
        MoveEffect = 1,
        Count
    }
}